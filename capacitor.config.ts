import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.academyShare',
  appName: 'academyShare',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
